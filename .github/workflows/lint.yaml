name: Lint
on: 
  pull_request:
    branches:
      main
  
jobs:

  eslint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: install modules
      run: npm install
    - name: run ESLint
      run: npx eslint . --ext .js,.jsx,.ts,.tsx
      